import React = require('react');

const DynamicComponentText = () => {
  // React.startTransition(() => {
  //   Array.from(Array(100 * 100 * 100 * 100).keys()).forEach((i) => i);
  // });
  return <div>Dynamic text component</div>;
};

export default DynamicComponentText;
